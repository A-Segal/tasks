import { StatusiconPipe } from './statusicon.pipe';

describe('StatusiconPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusiconPipe();
    expect(pipe).toBeTruthy();
  });
});
